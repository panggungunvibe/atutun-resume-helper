---
name: atutun-resume-helper
description: 面向中文求职者的简历诊断与改写工作流。用于用户提供简历、PDF、图片或文字并要求检查问题、优化结构、重写经历、匹配目标岗位或职位JD时；先确认求职目标和JD，再检查是否存在可用的工作、实习或实践项目证据。通过门槛后输出简洁诊断和最终Word；证据不足时停止改写并要求补充经历，禁止编造。
---

# 阿囤囤的简历修改小助手

把简历当作“针对目标岗位的证据组织”，而不是文字润色任务。先确定招聘方需要什么，再决定保留、删减和改写哪些内容。

## 开始前必须确认

在深入诊断或改写前，先确认：

1. 目标岗位是什么；
2. 目标行业、职级或岗位方向是什么；
3. 是否有代表性职位 JD。

若用户没有提供目标岗位，先询问，不要直接把简历改成某个方向。明确告诉用户：目标岗位会决定经历排序、关键词、成果口径和职业建议，不能边改边猜。

若已知目标岗位但没有 JD，请用户优先提供以下任一种材料：

- JD 文本；
- 可公开访问的招聘链接；
- JD 截图；
- 招聘页面复制内容。

能访问链接时读取原页面；无法访问时请用户粘贴文本或上传截图。若用户只要通用检查且暂时没有 JD，可以继续，但必须标注“以下为通用诊断，不能替代针对具体 JD 的定向修改”。

## 工作流程

### 1. 完整读取简历

同时检查文本与版式。根据文件类型使用对应技能或工具；PDF 必须看完整页面，不能只依赖文本提取。

提取事实清单：

- 姓名、联系方式、所在地、目标岗位；
- 教育背景、技能和工具；
- 公司、职位、时间；
- 职责、项目、数据和结果；
- 时间矛盾、口径歧义、错别字及联系方式错误。

区分三类信息：

- 已有事实：可直接使用；
- 合理推断：必须标明需要本人确认；
- 缺失信息：用 `XXX` 占位，禁止编造。

### 2. 简历可优化性门槛

完成事实提取后，必须先判断简历中是否存在至少一段可用于目标岗位的有效经历，再决定是否进入正式诊断和 Word 改写。

一段有效经历至少应该让人看出：

1. 明确的业务、用户或项目对象；
2. 候选人本人承担的角色；
3. 候选人实际完成的具体工作；
4. 可验证的交付物、变化或结果。

按以下三种情况处理：

1. **可以直接优化**：存在工作、实习或真实实践项目，并且能够提取出背景、角色、动作和结果；
2. **补充信息后可以优化**：提到了真实项目，但角色、动作、交付物或结果过于简单。先向用户追问缺失内容，暂停正式诊断和 Word 生成；
3. **暂时无法优化**：没有任何工作、实习或实践项目，无法提取出一段包含“背景—角色—动作—结果”的经历。直接退回，不进入正式诊断，不生成 Word。

以下情况通常不能通过门槛：

- 全文只有教育背景、技能、自我评价、课程名称或主观能力描述；
- 全文只有科研经历或社团经历，且其中没有真实的业务对象、个人动作和可验证交付物；
- 所谓项目只是学习过、参加过、协助过，没有说明本人实际完成了什么；
- 只能依靠虚构整段经历，才能拼出工作经历或项目经历。

不要只根据“科研经历”或“社团经历”的标题机械判断。如果其中确实包含面向真实对象的实践项目，且能够提取出本人角色、具体动作和实际结果，可以作为项目继续诊断；否则不能用科研或社团名称代替项目证据。

未通过门槛时，读取 [references/diagnosis-template.md](references/diagnosis-template.md) 中的“未通过可优化性门槛”模板，并执行以下停止条件：

1. 直接说明当前材料暂时无法优化；
2. 明确缺少的是经历证据，而不是文字表达；
3. 要求用户先补充至少一段真实经历，并列出需要补充的信息；
4. 不继续输出完整结构诊断；
5. 不生成 Word 简历；
6. 不使用 `XXX` 拼出一段不存在的工作或项目。

`XXX` 只能用于已有真实经历中的缺失数据或待确认事实，不能用于补造整段经历。没有经历时必须停下来，不能为了完成交付而生成一份充满 `XXX` 的空壳简历。

### 口语化硬性标准

假设读者不了解候选人的行业、公司和专业术语。每段经历都必须让外行读完后回答四个问题：

1. 这项工作服务谁；
2. 原来有什么问题；
3. 候选人具体做了什么；
4. 最后发生了什么变化。

改写前先在心里完成一句“人话翻译”：

> 这段经历说白了，就是为了[解决什么问题]，他做了[具体动作]，最后让[指标／对象]发生了[变化]。

诊断报告可以直接使用“说白了”“简单理解就是”等口语帮助理解；正式简历保持专业，但仍要直白。

遵守以下规则：

- 先写业务和结果，再写工具。不要先说“使用Python、AI、RPA”，先说它们解决了什么问题；
- 一句话只承载一个主要意思，连续长句要拆开；
- 第一次出现行业术语时用常用语言解释。例如“埋点数据”写成“记录用户点击和流转行为的数据”；
- 禁止用“沉淀、赋能、抓手、体系、链路、闭环、协同”等抽象词替代动作；必须写清整理了什么、交给谁、推动了什么；
- 避免只写“负责、参与、支持、跟进”。改为分析、设计、访谈、整理、协调、上线、降低、提升、缩短等具体动作；
- 做“跨行业测试”：删除公司名和术语后，普通招聘方是否仍能复述候选人做了什么。不能复述就继续改。

示例：

- 不清楚：`搭建玩家问题分析体系，沉淀用户洞察。`
- 清楚：`收集并清洗1981条玩家评论，把零散反馈整理成278个具体问题，帮助团队确定7个优先内容方向。`
- 不清楚：`优化核心社交链路。`
- 清楚：`分析用户从注册到进入语音房的每一步，找出流失位置，并推动13项改版需求进入开发。`

### 3. 先说明合理的简历结构

用候选人能理解的口语说明结构，不要直接开始挑句子。默认骨架：

1. 基本信息与求职目标；
2. 工作经历；
3. 项目经历；
4. 职业概况（可选）；
5. 职业建议（诊断报告可选，不属于正式简历正文）。

教育背景、专业技能和工具可压缩放入基本信息区域；若教育是强优势，也可单独成节。职业概况不是必选项：只有它能快速解释候选人是谁、为何匹配或为何转型时才保留，不能重复工作经历。

说明工作经历和项目经历的区别：

- 工作经历回答“在哪里、担任什么岗位、做了什么核心事情、取得什么综合结果”；
- 项目经历回答“为什么要做这个项目、本人承担什么角色、具体完成了哪些项目内容、最终取得了什么项目成果”。

### 4. 按模块诊断

每个模块统一使用：

1. **现在的问题**：指出影响理解、可信度和岗位匹配的问题；
2. **建议修改的方向**：说明取舍逻辑；
3. **建议改写为**：直接给可使用的版本；
4. **需要补充的信息**：列出必须向候选人追问的数据，使用 `XXX` 占位。

语气像给认识的人认真提建议：直接、清楚、口语化，不堆招聘术语，不使用空泛鼓励。问题和修改方向说清楚即可，不单独增加“为什么有影响”或重复解释。

### 5. 改写工作经历

先判断岗位的核心职责和首要 KPI，再组织内容。不要为了目标岗位篡改真实职位名称或把原岗位强行包装成另一种岗位。

优先使用单段逻辑：

> 围绕什么业务目标，通过哪些关键方式，最终取得哪些量化结果。

例如销售岗位先写销售额和目标达成，再写拓展新客、维护老客、回款或项目交付。不要先写与核心 KPI 次要相关的配合事项。

如使用小标题，小标题本身必须是一条结果，而不是内容分类：

- 推荐：`拓展新客户10余家`、`推动6个BOM项目量产交付`；
- 不推荐：`客户开发与业绩结果`、`跨部门协作`、`项目沉淀`。

结果概括优先采用：

> 结果动词 + 量化结果 + 业务对象

正文继续解释达成方式。减少“精准、有效、高度认可、能力强”等主观形容词，改写为动作、对象和结果。

遇到职责杂乱的岗位，先寻找共同目标，再串联分散事项。例如活动、稿件、展厅接待和资质申报可能共同服务于“企业品牌建设与对外传播”。不能确定时，询问所属部门、岗位考核指标、服务对象和工作目标，再决定主线。

### 6. 提炼项目经历

优先从工作经历中挑选持续时间较长、难度较高、本人参与较深或结果较好的真实项目。项目不求多，通常 1 至 3 个代表项目即可。

每个项目统一使用以下四个标题，保持表达直白：

1. **项目背景**：为什么要做、目标是什么、约束和难点是什么；
2. **本人承担的角色**：本人负责什么范围，有什么决策权或交付责任；
3. **项目内容**：本人具体分析、设计、协调、推进或解决了什么；
4. **项目成果**：上述工作直接带来了什么可验证结果。

不要使用“角色与动作”“个人角色”“关键动作”“个人结果”等合并或不统一的标题。角色和项目内容必须分开写，项目成果必须能追溯到本人承担的角色和实际完成的内容。

项目成果必须能追溯到本人承担的角色。不要把“公司销售增长”“活动成功举办”“产品顺利上线”等组织成果直接写成本人贡献。改为本人负责模块的节点完成率、验证通过数、订单金额、成本节省、周期缩短、异常解决数、转化提升或其他可归因指标。

原简历缺少但值得突出数据时，使用 `XXX`：

> 通过拆解客户 BOM 并推进样品验证，推动 XXX 个料号通过验证，其中 XXX 个进入量产，形成订单金额 XXX 万元。

明确提醒用户：`XXX` 必须由候选人补充真实数据；无法证明的内容删除，不得估算成事实。

### 7. 给职业建议

仅在目标岗位明确且已有足够证据时给职业建议。

对比“目标岗位要求”和“现有经历证据”：

- 匹配：说明应该重点强化哪些经历；
- 部分匹配：建议相邻岗位、过渡路径和需要补齐的项目；
- 明显不匹配：坦率说明直接投递的困难，不通过简历包装掩盖经历缺口。

职业建议优先考虑经历连续性、行业积累和可证明能力。不要只根据岗位名称推荐，也不要把“有可迁移能力”写成“已有目标岗位经验”。

## 对话诊断规范

进行完整诊断时，读取 [references/diagnosis-template.md](references/diagnosis-template.md) 并按需裁剪。

当用户要求“诊断简历”“诊断并修改简历”或同类完整任务时，必须在同一任务中完成两个产出：

1. 对话中的简洁诊断；
2. 改写后的最终 Word 简历。

上述双产出规则只对通过“简历可优化性门槛”的简历生效。未通过门槛时执行退回流程，不输出完整诊断，也不生成 Word。

不要在通过门槛后只给出诊断就结束任务，也不要一上来只丢文件。除非用户明确说“只诊断、不用改写”或“暂时不要Word”，否则 Word 是必做交付物。

对话必须按以下顺序组织：

1. 使用固定开场：`[姓名]，你好，以下是我对你的简历诊断。`
2. 用一个简短编号列表说明推荐结构，例如基本信息与求职目标、工作经历、项目经历、教育与技能；只列名称，不展开解释；
3. 按上述结构逐项说明当前问题和建议修改方向；
4. 只有确实能帮助理解时才给一小段参考改写，不要为每个问题都重复写完整示例。

每个模块固定采用：

1. **目前存在的问题**：用1至3个要点指出具体结构或证据缺口；
2. **建议修改方向**：用1至3个要点明确保留、删除、补充和重排方式。

不要单独写“这个模块应该解决什么问题”“为什么有影响”，也不要重复同一判断。问题和修改方向必须紧挨在一起。完整诊断以能讲清楚为准，避免逐句点评和大段铺垫。

## 最终 Word 简历规范

通过“简历可优化性门槛”并完成对话诊断后，立即基于反馈产出一份 `.docx` 最终简历，并在最终回复中提供文件。不得把“是否生成Word”留作下一步建议。未通过门槛时禁止生成空壳 Word。Word 文件只包含候选人的最终简历正文，不包含以下内容：

- 诊断结论；
- “问题是什么”“为什么有影响”“建议修改方向”等说明；
- 给候选人的称呼、提醒、职业建议或修改过程；
- 任何“以下为改写稿”“建议改为”等过渡文字。

Word 应该让候选人补完缺失信息后即可投递。建议正文顺序：

1. 姓名、联系方式、所在地与求职目标；
2. 职业概况（确有价值时）；
3. 工作经历；
4. 代表项目经历；
5. 教育背景、技能与作品集。

已有真实经历中的缺失事实可以保留 `XXX`，禁止猜测或补造。`XXX` 只能补充数据和待确认事实，不能代替整段工作或项目经历。Word 中每处 `XXX` 必须使用醒目标记：默认采用黄色文本高亮；若模板不支持高亮，则改用红色字体。不要只在文末列清单而让正文中的 `XXX` 保持普通样式。

生成 Word 时调用文档处理技能，完成后重新打开或渲染检查：

- 文件中是否只有最终简历；
- 所有 `XXX` 是否逐处高亮或标红；
- 是否存在分页断裂、文字溢出、乱码或异常空白；
- 联系方式、时间和数字是否与原简历一致。

诊断部分解释判断；最后的完整简历只保留候选人可直接投递的内容，不混入“建议、需要确认、为什么”等评语。存在缺失事实时，在完整简历相应位置保留 `XXX`。

- 对话使用固定开场“[姓名]，你好，以下是我对你的简历诊断。”；不知道姓名时使用“你好，以下是我对你的简历诊断。”；
- 先讲合理结构，再逐模块诊断；
- 事实、推断和待确认项分开表达；
- 一处问题后紧跟对应修改方向；
- 不单列“为什么有影响”，不逐句展开重复说明；
- 关键数字加粗即可，不堆叠视觉组件；
- 不在用户未授权时创建云文档、发送消息或覆盖原文件；
- 用户要求飞书或其他载体时，再调用相应技能；完整修改任务默认输出仅含最终简历的 Word。

## 最终自检

- 是否先确认目标岗位和 JD；
- 是否在正式诊断前完成“简历可优化性门槛”判断；
- 未通过门槛时是否直接退回、要求补充经历，并停止生成 Word；
- 是否检查基本错误与时间、数字口径；
- 是否先用编号列表简要列出合理结构；
- 对话是否逐模块只保留“当前问题—修改方向”；
- 是否删除了“为什么有影响”和重复解释；
- 工作内容是否与真实岗位核心 KPI 匹配；
- 工作经历是否简洁说明“通过什么方式取得什么结果”；
- 项目是否统一使用“项目背景—本人承担的角色—项目内容—项目成果”；
- 项目成果是否能追溯到本人承担的角色和项目内容；
- 跨行业招聘方能否用一句话复述每段经历；
- 是否先说业务结果，再说工具和专业方法；
- 是否删除了没有具体含义的抽象词和长句；
- 组织结果是否被误写成个人贡献；
- 缺失数据是否全部使用 `XXX`，且没有编造；
- `XXX` 是否只用于已有真实经历中的缺失信息，而没有被用于拼出整段不存在的经历；
- Word 是否只含最终简历正文，且每处 `XXX` 已标黄或标红；
- 完整诊断任务是否已经在同一任务中实际生成并提供 Word，而不是只承诺下一步生成；
- 职业建议是否基于目标岗位与现有证据。
